{
  "name": "gridrush-arena",
  "version": "0.1.0",
  "private": true,
  "description": "PWA puzzle battle con multiplayer locale via server Node.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check app.js && node --check server.js"
  }
}
